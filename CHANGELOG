## Version 3.1
 * Corrected mistakes introduced in 3.0
 * Added function `getAgeModel`.
 * Harmonized function names (`findSampleDepth` and `findLSR`)

## Version 3.0
 * Changed `nsbConnect` to use the Zenodo archive as the website and server are defunct.
 * Added functions `lsr`, `getStratData` and `sample.depth`.

---
## Version 2.2
 * Switched to `RPostgres` to avoid the SHAM authentication issue.

---
## Version 2.1
 * Modified `getNeptuneData` to be conform with what [the NSB website](http://nsb-mfn-berlin.de) would produce.
 * Create helper function `.reverseSyn`.
 * Added function `changeAgeScale`.

---
## Version 2.0
 * Added `findAge`, `findPaleocoordinates`, `makeCoreFile`, `nsbConnect`.
 * Got rid of `getNeptuneTaxonMetaData` and `getNeptuneAgeModelMetaData`.
